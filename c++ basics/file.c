#include<stdio.h>
#include<conio.h>

int main()
{
    /*
    // N pattern
    for(int i=0;i<=10;i++)
    {
        for(int j=0;j<=10;j++)
        {
            if(j==0 || j==10 || j==i)
                printf("*");
            else 
                printf(" ");
        }
        printf("\n");
    }

    printf("\n");

    // G pattern
    for(int i=0;i<=10;i++)
    {
        for(int j=0;j<=10;j++)
        {
            if(i==0|| j==0 || (i==10 && j<=5) || (j==5 && i>=5) || (i==5 && j>=5)|| (j==10 && i>=5))
                printf("*");
            else
                printf(" ");
        }
        printf("\n");
    }

    printf("\n");

    // figure pattern
    for(int i=0;i<11;i++)
    {
        for(int j=0;j<11;j++)
        {
            if( ((i==0||i==3) && (j>=3 && j<=7)) || (j==3 && (i>=3 && i<=8)))
                printf("*");
            else
                printf(" ");
        }
        printf("\n");
    }
    */

    
    return 0;
}